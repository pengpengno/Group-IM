# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Disable configuration cache for Android tasks to avoid issues with JDK image transformation
org.gradle.configuration-cache=false

# Enable parallel builds
org.gradle.parallel=true

# Enable build caching
org.gradle.caching=true

# Enable experimental JS Canvas target
org.jetbrains.compose.experimental.jscanvas.enabled=true

# Optimize configuration performance
org.gradle.configureondemand=true

# å°è¯è§£å³Composeèµæºçæå¨é®é¢
kotlin.native.cacheKind=none
org.jetbrains.compose.experimental.macos.enabled=true
org.jetbrains.compose.experimental.uikit.enabled=true

# å³é­æé®é¢çèµæºçæå¨åè½
org.jetbrains.compose.resources.generateResourceAccessors=false